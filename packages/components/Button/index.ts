import { withInstall } from '@acorn-ui/utils'

import Button from './Button.vue'
import ButtonGroup from './ButtonGroup.vue'

export const AButton = withInstall(Button)
export const AButtonGroup = withInstall(ButtonGroup)

export * from './types'
