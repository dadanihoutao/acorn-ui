import { AButton, AButtonGroup, AIcon, ACollapse, ACollapseItem } from '@acorn-ui/components'

import type { Plugin } from 'vue'

export default [AButton, AButtonGroup, AIcon, ACollapse, ACollapseItem] as Plugin[]
